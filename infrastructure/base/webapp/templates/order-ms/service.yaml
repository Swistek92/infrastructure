apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp.fullname" . }}-order
  labels:
    {{- include "webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.order.service.type | default "ClusterIP" }}
  selector:
    app.kubernetes.io/name: order-ms
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.order.service.port | default 3000 }}
      targetPort: {{ .Values.order.containerPort | default 3000 }}
